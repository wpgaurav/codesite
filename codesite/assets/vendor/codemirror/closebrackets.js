// CodeMirror closebrackets addon - Placeholder
// Download from: https://codemirror.net/5/addon/edit/closebrackets.js
(function(mod) {
    if (typeof exports == "object" && typeof module == "object")
        mod(require("../../lib/codemirror"));
    else if (typeof define == "function" && define.amd)
        define(["../../lib/codemirror"], mod);
    else
        mod(CodeMirror);
})(function(CodeMirror) {
    // Placeholder - auto close brackets functionality
});
